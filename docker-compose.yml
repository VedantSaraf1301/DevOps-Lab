version: '3.8'

services:
  server:
    build:
      context: ./server
    image: YOUR_DOCKERHUB_USERNAME/inkwell-server:latest
    container_name: inkwell-server
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=${MONGO_URI}
    restart: unless-stopped

  client:
    build:
      context: ./client
    image: YOUR_DOCKERHUB_USERNAME/inkwell-client:latest
    container_name: inkwell-client
    ports:
      - "80:80"
    depends_on:
      - server
    restart: unless-stopped
